"""Phase 7 real-model pilot infrastructure.

The package is credential-free by default. Remote and local adapters expose
validation and safety-gate contracts; CI and normal tests use the mock adapter.
"""

