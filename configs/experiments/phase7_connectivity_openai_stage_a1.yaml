pilot_id: phase7_connectivity_openai_stage_a1
stage: connectivity
provider_config: configs/providers/remote/openai_phase7_stage_a1.yaml
output_root: results/tables/phase7
task_roots: [scenarios]
domains: [privacy]
task_ids:
  - task_privacy_aggregate_only
architectures: [single_agent]
delegation_depths: [1]
branching_factors: [1]
behavior_conditions: [honest]
attacker_conditions: [none]
oversight_conditions: [none]
seeds: [1]
provider_calls_enabled: true
cache_enabled: true
resume_enabled: true
external_tools_enabled: false
max_cost_usd: 0.01
max_total_tokens: 3000
max_requests: 1
max_trajectories: 1
max_local_executions: 2
max_runs: 1
large_run_threshold: 1
human_review_sample_size: 0
notes: "Stage A.1 only: one OpenAI diagnostic request; second local execution only if cache-served."
