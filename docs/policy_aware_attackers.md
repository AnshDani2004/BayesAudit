# Policy-Aware Attackers

Policy-aware attackers may condition on configured public information such as policy family, public schedules, public audit budget, visible intervention history, and explicitly exposed monitor scores.

They may not observe future random draws, hidden labels, private monitor parameters, or unobserved branches unless the config uses evaluation-only tier 5. Tier 5 results are upper bounds, not deployable threat estimates.
