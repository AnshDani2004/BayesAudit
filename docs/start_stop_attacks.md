# Start-Stop Attacks

Start-stop attackers separate starting an attack from continuing, modifying, moving, concealing, aborting, or completing it.

The Phase 6 smoke implementation can start when expected utility is favorable, continue under low estimated detection probability, abort when feedback implies high detection risk, and preserve aborted and failed attacks as first-class artifacts.

Stop decisions are based only on attacker-observable feedback.
