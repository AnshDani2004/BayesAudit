# v1.0.0 Release Candidate

Status: merge-ready after manual review.

Do not create the `v1.0.0` annotated tag until this pull request is merged into `main` with a merge commit and local `main` is synced.
