# Provider Model Card

This legacy entry is retained for compatibility with Phase 10 validators. The current public-facing provider documentation is [Model and Provider Card](model_and_provider_card.md).

Provider: `openai`.
Model: `gpt-5-nano-2025-08-07`.
Usage: non-streaming Responses API with native structured output and no external tools.
